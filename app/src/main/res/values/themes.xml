<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculadora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- <item name="colorPrimary">@or/my_light_primary</item> -->
    </style>


    <style name="Theme.Calculadora" parent="Base.Theme.Calculadora" />
    <style name="botones" parent="Widget.Material3.Button">
        <!-- Set a drawable as the background -->
        <item name="android:background">@drawable/basic_button</item>
        <item name="backgroundTint">@null</item>
        <!-- Customize other properties as needed -->
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:textSize">20dp</item> <!-- Text size -->
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">2dp</item><!-- Padding -->"

    </style>
    <style name="botones_num" parent="Widget.Material3.Button">
        <!-- Set a drawable as the background -->
        <item name="android:background">@drawable/button_selector_num</item>
        <item name="backgroundTint">@null</item>
        <!-- Customize other properties as needed -->
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:textSize">20dp</item> <!-- Text size -->
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">2dp</item><!-- Padding -->"
    </style>
    <style name="botones_operadores" parent="Widget.Material3.Button">
        <!-- Set a drawable as the background -->
        <item name="android:background">@drawable/button_selector_oper</item>
        <item name="backgroundTint">@null</item>
        <!-- Customize other properties as needed -->
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:textSize">20dp</item> <!-- Text size -->
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">2dp</item><!-- Padding -->"
    </style>
    <style name="botones_util" parent="Widget.Material3.Button">
        <!-- Set a drawable as the background -->
        <item name="android:background">@drawable/button_selector_util</item>
        <item name="backgroundTint">@null</item>
        <!-- Customize other properties as needed -->
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:textSize">20dp</item> <!-- Text size -->
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">2dp</item><!-- Padding -->"
    </style>
    <style name="botones_memo" parent="Widget.Material3.Button">
        <!-- Set a drawable as the background -->
        <item name="android:background">@drawable/button_selector_memo</item>
        <item name="backgroundTint">@null</item>
        <!-- Customize other properties as needed -->
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:textSize">20dp</item> <!-- Text size -->
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">2dp</item><!-- Padding -->"
    </style>



</resources>

